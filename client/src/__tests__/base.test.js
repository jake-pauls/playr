describe('base', () => {
  test('logic', () => {
    expect(1 + 1).toBe(2);
  });

  test('reason', () => {
    expect(1).not.toBe(2);
  });

  test('understanding', () => {
    expect(2).toBe(2);
  });
});
